{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "uvicorn cabi_outfits.api:app --host 0.0.0.0 --port $PORT --app-dir src",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 300
  }
}
